interface:
  display_name: "UI Forge"
  short_description: "Design premium, proof-led interfaces"
  default_prompt: "Use $ui-forge to design and implement this interface. Set a visual ambition, avoid generic hero/card/gradient defaults, create a memorable product-specific hook, show truthful proof, cover real states, and verify responsive/accessibility quality."

policy:
  allow_implicit_invocation: true
