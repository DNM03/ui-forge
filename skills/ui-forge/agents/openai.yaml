interface:
  display_name: "UI Forge"
  short_description: "Generate elegant, product-derived interfaces"
  default_prompt: "Use $ui-forge to generate and implement this interface. Resolve the product thesis, audience, content, and assets; explore three materially different visual directions; select one product-derived central move and macrostructure; choose one primary visual carrier, a coherent theme, type roles, color deployment, and section rhythm; budget one display apex and the first viewport before sizing type; suppress unsupported genre defaults and recent house-style motifs; implement truthful subject evidence and complete states; then make one craft refinement pass and verify blocking responsive, accessibility, behavior, and honesty constraints."

policy:
  allow_implicit_invocation: true
